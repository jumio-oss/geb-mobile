buildscript {
    repositories {
        mavenLocal()
        mavenCentral()
    }
}

def branch = System.getProperty('branch','master')
group = 'jumio.oss'
version = (branch == 'master' ? '1.1-SNAPSHOT' : branch )

